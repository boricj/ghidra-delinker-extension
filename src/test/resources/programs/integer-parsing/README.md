# integer-parsing test case

This is a simple parser-focused test case for the delinker.
It is designed specifically to exercise relocations with negative addends.

## Execution

Any freelist-allocator executable artifact should print "All tests passed." and return 0 when run, if the test suite passed successfully.
