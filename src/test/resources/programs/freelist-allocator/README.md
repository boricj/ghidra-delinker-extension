# freelist-allocator test case

This is a simple allocator-focused test case for the delinker.
It is designed specifically to exercise relocations with non-zero addends and uninitialized data.

## Execution

Any freelist-allocator executable artifact should print "All tests passed." and return 0 when run, if the test suite passed successfully.
